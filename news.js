fetch("data/news.json").then(r=>r.json()).then(x=>{const f=document.getElementById("feed");x.forEach(n=>f.insertAdjacentHTML("beforeend",`<article class=card><span class=tag>${n.tag}</span><span class=muted> ${n.date} · ${n.asset}</span><h2>${n.headline}</h2><p class=muted>${n.summary}</p><div class=notice>Does this change your investment thesis, or only the market price?</div></article>`))})