const K="mm-pfs";const load=()=>JSON.parse(localStorage.getItem(K)||"[]"),save=x=>localStorage.setItem(K,JSON.stringify(x));function render(){const box=document.getElementById("pf-list"),x=load();box.innerHTML=x.length?"":"<p class=muted>No portfolios yet.</p>";x.forEach((p,i)=>{box.insertAdjacentHTML("beforeend",`<div class=portfolio-row><div><strong>${p.name}</strong><small>€${p.capital.toLocaleString()} · ${p.style}</small></div><button class="btn ghost" onclick=resetPF(${i})>Reset</button></div>`)});}function resetPF(i){const x=load();x[i].capital=x[i].start;save(x);render()}document.getElementById("pf-create").onclick=()=>{const c=Number(document.getElementById("pf-capital").value);if(c<1000||c>1e9){document.getElementById("pf-msg").textContent="Choose €1,000 to €1B";return}const x=load();x.push({name:document.getElementById("pf-name").value||"New Portfolio",capital:c,start:c,style:document.getElementById("pf-style").value});save(x);render()};render();