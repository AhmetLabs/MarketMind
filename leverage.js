function m(v){return `€${Math.round(v).toLocaleString()}`}function calc(){const e=+eq.value,l=+lev.value,r=+move.value/100,x=e*l,p=x*r,z=e+p;moveLabel.textContent=`${move.value}%`;exposure.textContent=m(x);pnl.textContent=m(p);pnl.className=p>=0?"positive":"negative";ending.textContent=m(Math.max(0,z));warning.textContent=z<=0?"This simulated move wipes out the equity.":l>=3?"High leverage: a small adverse move creates a large equity loss.":"Lower leverage reduces amplification, but risk remains."}[eq,lev,move].forEach(x=>x.oninput=calc);calc();